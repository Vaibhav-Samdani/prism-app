export async function GET() {

    // Check whether the user is logged in
    // Get the user id 
    // search for the database with the same user id 


  return Response.json({
    projectName: "Next.js",
  });
}
